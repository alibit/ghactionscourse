name: Frist Workflow

on:
  push:
    branches:
      - main
      
jobs:
  example-job:
    runs-on: ubuntu_latest

    steps:
      - name: Print a welcome message 
        run: echo "Welcome to our first GitHun Actions Workflow! By Ali Nabil"
